let nota = 7; 
let avaliação;

if (nota >= 8) {
    avaliação = "Muito bom";
} else if (nota >= 6) {
    avaliação = "Satisfeito";
} else {
    avaliação = "Insuficiente";
}

console.log(avaliação);